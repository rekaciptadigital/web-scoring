import * as React from "react";

import { SubNavbar } from "../components/submenus-settings";

function PageEventBudRests() {
  const eventId = 22;
  return (
    <div>
      <SubNavbar eventId={eventId} />
    </div>
  );
}

export default PageEventBudRests;
