import * as React from "react";
import FormSheet from "../FormSheet";

// eslint-disable-next-line no-unused-vars
function StepJadwal({ eventData, updateEventData, validationErrors }) {
  return (
    <FormSheet>
      <h1>TBD: Form Jadwal Pertandingan</h1>
    </FormSheet>
  );
}

export { StepJadwal };
